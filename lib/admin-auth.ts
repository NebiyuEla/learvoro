import{env}from'@/lib/runtime';import{cookies}from'next/headers';
const runtime=()=>env as unknown as Record<string,string|undefined>;const enc=new TextEncoder();
async function signature(value:string){const secret=runtime().ADMIN_SESSION_SECRET;if(!secret)return'';const key=await crypto.subtle.importKey('raw',enc.encode(secret),{name:'HMAC',hash:'SHA-256'},false,['sign']);const bytes=new Uint8Array(await crypto.subtle.sign('HMAC',key,enc.encode(value)));return Array.from(bytes).map(x=>x.toString(16).padStart(2,'0')).join('')}
export async function createAdminToken(){const expires=Date.now()+8*60*60*1000;const value=`admin.${expires}`;return`${value}.${await signature(value)}`}
export async function isAdmin(){const token=(await cookies()).get('learvoro_admin')?.value;if(!token)return false;const parts=token.split('.');if(parts.length!==3||parts[0]!=='admin'||Number(parts[1])<Date.now())return false;const expected=await signature(`admin.${parts[1]}`);if(!expected||expected.length!==parts[2].length)return false;let result=0;for(let i=0;i<expected.length;i++)result|=expected.charCodeAt(i)^parts[2].charCodeAt(i);return result===0}
export function adminConfigured(){return!!(runtime().ADMIN_EMAIL&&runtime().ADMIN_PASSWORD&&runtime().ADMIN_SESSION_SECRET)}
export function validAdminCredentials(email:string,password:string){const r=runtime();return adminConfigured()&&email===r.ADMIN_EMAIL&&password===r.ADMIN_PASSWORD}
