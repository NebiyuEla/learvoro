'use client';import{useState}from'react';import{CheckCircle2,Loader2}from'lucide-react';export function CompleteLessonButton({courseSlug,lessonSlug,initial=false}:{courseSlug:string;lessonSlug:string;initial?:boolean}){const[done,setDone]=useState(initial);const[saving,setSaving]=useState(false);async function toggle(){setSaving(true);const r=await fetch('/api/progress',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({courseSlug,lessonSlug,completed:!done})});if(r.ok)setDone(!done);setSaving(false)}return <button onClick={toggle} disabled={saving} className={`rounded-lg border px-4 py-2.5 text-sm font-semibold ${done?'border-[#18B394] bg-[#e9f8f2] text-[#087861]':''}`}>{saving?<Loader2 className="mr-2 inline animate-spin" size={17}/>:<CheckCircle2 className="mr-2 inline" size={17}/>} {done?'Completed':'Mark complete'}</button>}
