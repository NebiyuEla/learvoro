'use client';import{useState}from'react';import{Loader2,LockKeyhole}from'lucide-react';
export function AdminLogin(){const[status,setStatus]=useState<'idle'|'busy'|'error'|'config'>('idle');async function submit(e:{preventDefault():void;currentTarget:HTMLFormElement}){e.preventDefault();setStatus('busy');const form=new FormData(e.currentTarget);const response=await fetch('/api/admin/session',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email:form.get('email'),password:form.get('password')})});if(response.ok)location.reload();else setStatus(response.status===503?'config':'error')}return <form onSubmit={submit} className="mt-8 space-y-5"><label className="block text-sm font-semibold">Admin email<input name="email" type="email" required autoComplete="username" className="field"/></label><label className="block text-sm font-semibold">Password<input name="password" type="password" required autoComplete="current-password" className="field"/></label><button disabled={status==='busy'} className="flex w-full items-center justify-center gap-2 rounded-lg bg-[#0757B2] p-3.5 font-semibold text-white">{status==='busy'?<Loader2 className="animate-spin" size={18}/>:<LockKeyhole size={18}/>}Sign in to admin</button>{status==='error'&&<p role="alert" className="rounded-lg bg-red-50 p-3 text-sm text-red-700">The email or password is incorrect.</p>}{status==='config'&&<p role="alert" className="rounded-lg bg-amber-50 p-3 text-sm text-amber-800">Admin access needs its three server environment values configured.</p>}</form>}
