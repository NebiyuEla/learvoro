import { cookies } from 'next/headers';
export async function GET(request:Request){const id=process.env.GOOGLE_CLIENT_ID;if(!id)return Response.redirect(new URL('/login?error=google_not_configured',request.url));const url=new URL(request.url),state=crypto.randomUUID(),returnTo=url.searchParams.get('returnTo')||'/my-learning';(await cookies()).set('google_oauth',JSON.stringify({state,returnTo}),{httpOnly:true,secure:process.env.NODE_ENV==='production',sameSite:'lax',path:'/',maxAge:600});const callback=new URL('/api/auth/google/callback',process.env.APP_URL||url.origin);const auth=new URL('https://accounts.google.com/o/oauth2/v2/auth');auth.searchParams.set('client_id',id);auth.searchParams.set('redirect_uri',callback.toString());auth.searchParams.set('response_type','code');auth.searchParams.set('scope','openid email profile');auth.searchParams.set('state',state);return Response.redirect(auth)}
